== README

There are two different versions that was made for the Grocery-Quiz.

1) On the main branch, a version that doesn't use AJAX but rather a recursive algorithm to cycle through the questions.

2) On the usingAJAX branch, instead of a recursive algorithm, it uses AJAX to cycle through the questions.
